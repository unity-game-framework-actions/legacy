name: 'UGF.Changelog'
description: 'No description.'
inputs:
  token:
    default: ${{ github.token }}
  milestones-request:
    default: closed
  issues-request:
    default: closed
  config-path:
    default: ./config.json
runs:
  using: 'node12'
  main: 'dist/index.js'